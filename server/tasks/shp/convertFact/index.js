// Преобразовать фактические данные

const convertTechnology = require('../convertTechnology/')

module.exports = function({ data }) {
    const fact = { ...data }
    /*
    const joint = Object.entries(fact).map(item => {
        console.log(item)
    })
    */


   // console.log(convertTechnology())
}